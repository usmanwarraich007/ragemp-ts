/**
 * garage-browse module — Player garage vehicle list + retrieve page.
 * Page id: 'garage-browse'
 */
import { createModule } from '@/core';
import GarageBrowse from './GarageBrowse.vue';

createModule('garage-browse', GarageBrowse);
