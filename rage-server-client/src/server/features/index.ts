/**
 * Feature barrel — add an import here for each new server feature.
 *
 * Convention: keep one file per feature in server/features/.
 * Example:
 *   import './auth/auth.feature';
 *   import './character/character.feature';
 */

// Built-in — remove when replacing with real features
import '../hello-world/hello-world';
import '../hello-world/second-hello-world';
